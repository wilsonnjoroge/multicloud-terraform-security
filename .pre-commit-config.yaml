
repos:
  - repo: local
    hooks:
      - id: gitleaks
        name: Detect hardcoded secrets
        entry: gitleaks protect --staged
        language: system
        pass_filenames: false

      - id: semgrep
        name: Semgrep SAST
        entry: semgrep --config p/default
        language: system